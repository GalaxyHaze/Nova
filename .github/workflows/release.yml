name: Build and Release

on:
  push:
    tags:
      - 'v*' # Trigger this workflow when a tag starting with 'v' is pushed (e.g., v1.0.0)

jobs:
  build-and-release:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target_name: nova-linux-amd64
          - os: macos-latest
            target_name: nova-macos-amd64
          - os: windows-latest
            target_name: nova-windows-amd64.exe

    steps:
      # 1. Checkout Code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Setup CMake (Not strictly needed on latest runners, but good practice)
      - name: Setup CMake
        uses: jwlawson/actions-setup-cmake@v2

      # 3. Configure & Build
      - name: Configure CMake
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release

      # 4. Rename and Prepare Artifacts
      # CLion/CMake usually outputs the binary as the project name.
      # We need to rename it to match your install.sh expectations.
      - name: Prepare Artifacts
        shell: bash
        run: |
          mkdir -p artifacts
          if [ "${{ matrix.os }}" == "windows-latest" ]; then
            # Windows usually outputs in build/Release/
            cp build/Release/Nova.exe artifacts/${{ matrix.target_name }}
          else
            # Linux/Mac usually outputs in build/
            cp build/Nova artifacts/${{ matrix.target_name }}
          fi

      # 5. Upload to GitHub Release
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: artifacts/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}